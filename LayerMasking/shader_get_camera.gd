#extends Node
#
##@onready var cam : Camera2D;
#@onready var level : SubViewport = $RedLayer/LevelVisual
#@onready var shader : Material = "res://Levels/Level13D.tscn::ShaderMaterial_b8h2f"
#
#### Called when the node enters the scene tree for the first time.
##func _ready() -> void:
	##pass # Replace with function body.
#
## Called every frame. 'delta' is the elapsed time since the previous frame.
#func _process(delta: float) -> void:
	#var cam : Camera3D = game_manager.player.camera
	#
	#var cam_size : Vector2 = get_viewport().get_visible_rect().size / Vector2(level.size);
	#var cam_pos : Vector2 = get_viewport().get_visible_rect().position / Vector2(level.size);
	#print(cam_size);
	#print(cam_pos);
	##Materia._set
